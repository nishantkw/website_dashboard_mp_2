/** @deprecated Use schemaRegistry.js — kept for backward-compatible imports */
export {
  getImportTables,
  getImportTable,
  assertSafeIdent,
  validateMappedRows,
  normalizeCell,
  suggestMapping,
} from './schemaRegistry.js'
