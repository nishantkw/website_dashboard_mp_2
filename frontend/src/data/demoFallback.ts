/**
 * When false, pages never show mock/demo data if the backend is unreachable.
 * Offline → empty charts/tables + error status (no temporary preview).
 */
export const ENABLE_DEMO_FALLBACK = false
